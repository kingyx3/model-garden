apiVersion: v1
kind: Service
metadata:
  name: model-garden-gateway
  namespace: model-garden
spec:
  type: LoadBalancer
  selector:
    app: model-garden-gateway
  ports:
    - name: http
      port: 80
      targetPort: http
